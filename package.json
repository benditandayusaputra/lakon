{
  "name": "lakon",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "pnpm --filter @lakon/web dev",
    "build": "pnpm --filter @lakon/web build",
    "check:signs": "node tools/check-signs.mjs",
    "typecheck": "tsc -b --pretty",
    "test": "vitest run",
    "e2e": "pnpm --filter @lakon/web e2e",
    "db:push": "pnpm --filter @lakon/web db:push"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vitest": "^3.0.5"
  }
}
